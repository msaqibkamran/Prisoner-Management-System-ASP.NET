@model IEnumerable<WebProject1.DAL.imprisonment_record>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.jail_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imprisonment_duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.reason_of_transfer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.entry_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.exit_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prisoner.name)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.jail_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imprisonment_duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.reason_of_transfer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.entry_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.exit_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.prisoner.name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
